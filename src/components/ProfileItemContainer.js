import React, { Component } from 'react';
import {
  View,
} from 'react-native';
import styles from './styles';

class ProfileItemContainer extends Component {
  render(){
    <View style={style.profileItemContainer} />
  }
}

export default ProfileItemContainer;
