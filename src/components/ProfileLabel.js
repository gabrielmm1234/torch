import React, { Component } from 'react';
import {
  Text,
} from 'react-native';
import styles from './styles';

class ProfileLabel extends Component {
  render(){
    <Text style={styles.profileLabelStyle} />
  }
}

export default ProfileLabel;
